server:
  port: ${SERVER_PORT:8089}
  shutdown: graceful # aguarda requests terminarem antes de matar o processo

application:
  security:
    jwt:
      secret-key: ${JWT_SECRET}

spring:
  lifecycle:
    timeout-per-shutdown-phase: 20s # tempo máximo de espera
  config:
    import: "optional:classpath:common-application.yml"
  application:
    name: audit-service
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:ms_audit_db}
    username: ${DB_USER:mysql_audit_user}
    password: ${DB_PASSWORD:mysql_audit_password}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
  rabbitmq:
    host: ${SPRING_RABBITMQ_HOST:localhost}
    port: ${SPRING_RABBITMQ_PORT:5672}
    username: ${SPRING_RABBITMQ_USERNAME:guest}
    password: ${SPRING_RABBITMQ_PASSWORD:guest}
  flyway:
    enabled: true
    baseline-on-migrate: true
    baseline-version: 0
    locations: classpath:db/migration

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka/}
  instance:
    hostname: ${HOSTNAME:localhost}
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  metrics:
    tags:
      application: ${spring.application.name}
  tracing:
    sampling:
      probability: 1.0
  zipkin:
    tracing:
      endpoint: "http://zipkin:9411/api/v2/spans"

app:
  security:
    encryption-key: ${ENCRYPTION_KEY:ThisIsAFixedKeyForEncryption32Ch}
